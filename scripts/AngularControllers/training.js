var app = angular.module('SHP-Web', []);
app.controller('TrainingCtrl', function($scope, $http) {
	$scope.Title = "Training";

    $scope.loaded = false;
    
    //When Data is loaded
    $scope.loaded = true;
   
});