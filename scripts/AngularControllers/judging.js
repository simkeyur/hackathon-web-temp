var app = angular.module('SHP-Web', []);
app.controller('JudgingCtrl', function($scope, $http) {
	$scope.Title = "Judging";

    $scope.loaded = false;
    
    //When Data is loaded
    $scope.loaded = true;
   
});